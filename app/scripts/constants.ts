enum Constants {
    ELEMENTARY_CHARGE = -1.6 * Math.pow(10, -19),
    ELECTRON_MASS = 9.1093837 * Math.pow(10, -31),
    PI = Math.PI,
    VACUUM_PERMITTIVITY = 8.86 * Math.pow(10, -12),
    ELECTRIC_CONSTANT = 9 * Math.pow(10, 9)
}

export default Constants